{
  "name": "my-service",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "start": "nodemon --exec \"sls offline start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "syncclient": "cd client && aws s3 sync .  s3://lambdastatic5/",
    "deploy": "sls deploy && cd client && aws s3 sync .  s3://lambdastatic5/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.646.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.2",
    "serverless-offline": "^6.1.2"
  }
}
